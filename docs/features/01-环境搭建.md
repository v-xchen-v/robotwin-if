---
status: in-progress
parent: "[[RoboTwin-IF 复刻]]"
tags: [robotwin, vla, benchmark]
---

# 阶段1：环境搭建 + 跑通基线

对应主文档「时间评估」阶段1，预估 1 – 1.5 人天。

## 目标 / 完成标准

1. **原生 pipeline 跑通**：远程 Linux GPU 机器上用现成安装脚本装好 RoboTwin 2.0（commit `0aeea2d669c0f8516f4d5785f0aa33ba812c14b4`），跑 `collect_data.sh click_bell demo_clean 0`（或类似简单原生任务）3-5个episode，确认输出目录有轨迹数据 + `instruction/episode_*.json` + 图像/视频。验证 SAPIEN渲染、运动规划(mplib/curobo)、物体资产加载、指令生成整条链路。
2. **桥接机制验证**（分两步，第一步轻量前置，第二步完整 smoke test）

### 关键发现：桥接点有3个，不是1个

调研 `collect_data.py` 源码发现，一个任务要跑起来实际有3处按 `task_name` 命名对齐的地方：

```python
def class_decorator(task_name):
    envs_module = importlib.import_module(f"envs.{task_name}")
    env_class = getattr(envs_module, task_name)   # 类名必须和 task_name 完全一致，不只是文件名
```

1. `envs/{task_name}.py`，文件内 `class {task_name}(Base_Task)` — 类名要和文件名/task_name完全一致
2. `description/task_instruction/{task_name}.json` — **静态**指令模板文件，本身就写死了 `seen`/`unseen` 两个数组（离线用MLLM生成好存成json，不是运行时动态切分——之前笔记里"运行时生成12条切分10 seen/2 unseen"的理解不准确，实际是提前生成好的静态文件）
3. 任务用到新物体时，`description/objects_description/{obj_id}_{name}/` 下要有对应物体描述（原生50任务复用现有120个物体基本够，我们的新任务如果用到新物体比如抽屉柜，需要新增）

这意味着桥接方案要同时处理 `envs/` 和 `description/task_instruction/` 两处注入，之前"只 symlink envs/ 里的任务文件"的方案不完整。

### Step 1（轻量前置）：只验证 envs/ 桥接点

把 `smoke_click_bell.py`（复制自 `click_bell.py`，只改文件名和类名为 `smoke_click_bell`）symlink 进 submodule 的 `envs/`，跑一行 Python：

```python
import importlib
m = importlib.import_module("envs.smoke_click_bell")
getattr(m, "smoke_click_bell")  # 不报错即通过
```

不跑仿真、不需要GPU渲染，几秒出结果，先确认"文件命名+类名对齐"这条规则理解对了。

### Step 2（完整 smoke test，紧随 Step 1 之后）

在 Step 1 通过基础上，追加把 `smoke_click_bell.json`（复制自 `click_bell.json`）symlink 进 submodule 的 `description/task_instruction/`，跑真正的：

```bash
collect_data.sh smoke_click_bell demo_clean 0
```

产出3-5个episode数据，验证整条链路（仿真+运动规划+指令生成+两处桥接）都通。这就是"环境搭建"阶段的最终验收标准。

## 实现记录

2026-08-19，提交 `9a6cec2`（"Set up RoboTwin env and validate task bridging (feature-01)"）。详细底稿见 [[2026-08-19-task-bridging/task-bridging-impl-verify]]，架构图见 [[2026-08-19-task-bridging/architecture]]。

- **Step 1（envs/ 桥接点，无仿真）**：`smoke_click_bell.py` symlink 进 submodule `envs/`，`importlib.import_module` + `getattr` 拿到 `Base_Task` 子类，通过。
- **Step 2（完整链路，含 task_instruction 桥接点）**：`bridge_tasks.sh` 注入 `smoke_click_bell.json` 后跑 `collect_data.py smoke_click_bell demo_smoke`，3/3 episode `check_success` 通过，产出 `data/episode*.hdf5` + `instructions/episode*.json` + `video/episode*.mp4` 齐全，指令内容确认源自注入模板。
- 顺带验证了目标1里的 SAPIEN 渲染、运动规划(mplib/curobo)、物体资产加载、指令生成整条链路（走的是 smoke_click_bell 而非原生 click_bell，逻辑逐字复制自 click_bell，仅改类名）。
- **未覆盖**：桥接点3（`description/objects_description/`，smoke 复用现有物体未 exercise）；评测侧桥接（`eval_policy.sh multitask`，留待 feature-08）；seen/unseen 模板池零重叠断言。

## 踩坑

- **桥接点有3个，不是1个**：一开始以为只需 symlink `envs/` 里的任务文件，调研 `collect_data.py` 源码后发现实际有3处按 `task_name` 命名对齐：`envs/{task_name}.py`（类名必须和文件名/task_name完全一致）、`description/task_instruction/{task_name}.json`（静态指令模板）、`description/objects_description/{obj_id}_{name}/`（新物体才需要）。
- **纠正此前的错误理解**："运行时生成12条切分成10 seen/2 unseen" 的说法不准确——`task_instruction/{task_name}.json` 是提前离线用 MLLM 生成好、写死在文件里的静态模板（seen 50条/unseen 10条），采集时只是按 `max_descriptions` 循环 `replace_placeholders` 展开到目标条数（如100），不是运行时动态切分数量。
- **两个桥接点时序独立**：指令生成是仿真**全部结束后**才起子进程跑（不是逐 episode），靠仿真阶段落盘的 `scene_info.json` 做 placeholder 替换——这也是 Step1（纯 import，秒级）和 Step2（完整仿真）能分开单独验证的原因。
